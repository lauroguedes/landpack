{
  "title": "Go",
  "description": "Discovery and client configuration for a Go server. The linked example shows login, state checks and ID-token verification. Add a fresh PKCE verifier per login and exchange the callback code with that stored verifier.",
  "language": "go",
  "code": "package auth\n\nimport (\n    \"context\"\n    \"os\"\n    \"github.com/coreos/go-oidc/v3/oidc\"\n    \"golang.org/x/oauth2\"\n)\n\nfunc Configure(ctx context.Context) (*oauth2.Config, *oidc.IDTokenVerifier, error) {\n    provider, err := oidc.NewProvider(ctx, os.Getenv(\"SSO_ISSUER\"))\n    if err != nil { return nil, nil, err }\n    config := &oauth2.Config{\n        ClientID: os.Getenv(\"SSO_CLIENT_ID\"),\n        ClientSecret: os.Getenv(\"SSO_CLIENT_SECRET\"),\n        RedirectURL: os.Getenv(\"SSO_REDIRECT_URI\"),\n        Endpoint: provider.Endpoint(),\n        Scopes: []string{oidc.ScopeOpenID, \"profile\", \"email\"},\n    }\n    verifier := provider.Verifier(&oidc.Config{ClientID: config.ClientID})\n    return config, verifier, nil\n}",
  "href": "https://laravel-sso.lauroguedes.dev/docs/integrating/openid-connect",
  "order": 3,
  "icon": "go",
  "package": "coreos/go-oidc/v3 + golang.org/x/oauth2",
  "source": "https://github.com/coreos/go-oidc/blob/v3/example/idtoken/app.go"
}
