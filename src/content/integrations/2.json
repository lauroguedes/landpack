{
  "title": "Node.js",
  "description": "Server-side configuration for Node.js 20+. In your login route, create and store a fresh PKCE verifier in the user\u2019s session. The library\u2019s linked example covers the authorization redirect and validated callback.",
  "language": "javascript",
  "code": "import * as oidc from 'openid-client';\n\nconst config = await oidc.discovery(\n  new URL(process.env.SSO_ISSUER),\n  process.env.SSO_CLIENT_ID,\n  process.env.SSO_CLIENT_SECRET,\n);\n\n// Generate separately for each login; save in the user's session.\nconst verifier = oidc.randomPKCECodeVerifier();\nconst challenge = await oidc.calculatePKCECodeChallenge(verifier);\nconst authorizationUrl = oidc.buildAuthorizationUrl(config, {\n  redirect_uri: process.env.SSO_REDIRECT_URI,\n  scope: 'openid profile email',\n  code_challenge: challenge,\n  code_challenge_method: 'S256',\n});",
  "href": "https://laravel-sso.lauroguedes.dev/docs/integrating/openid-connect",
  "order": 1,
  "icon": "nodejs",
  "package": "openid-client v6",
  "source": "https://github.com/panva/openid-client/blob/main/examples/oidc.ts"
}
